<!-- Created by Phạm Duy – Giải pháp tự động hóa thông minh. -->
<Window x:Class="SmartMacroAI.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource ui_Pwd_Title}"
        Height="200" Width="340"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E2E"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TxtPrompt" Text="{DynamicResource ui_Pwd_Prompt}"
                   FontSize="13" Foreground="#CDD6F4" Margin="0,0,0,12"/>

        <PasswordBox Grid.Row="1" x:Name="PwdInput" Height="36" FontSize="14"
                    Background="#313244" Foreground="#CDD6F4"
                    BorderBrush="#45475A" Padding="10,8"
                    KeyDown="PwdInput_KeyDown"/>

        <TextBlock Grid.Row="2" x:Name="TxtError" Foreground="#F38BA8"
                   FontSize="11" Margin="0,8,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="{DynamicResource ui_CancelBtn}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="BtnCancel_Click" Margin="0,0,8,0"/>
            <Button Content="{DynamicResource ui_ConfirmBtn}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="BtnConfirm_Click"/>
        </StackPanel>
    </Grid>
</Window>
